### 분산된 오픈소스 UI 스니펫을 수집하고 검색부터 미리보기까지 연결한 개발자 도구

CSSMasterLibrary는 여러 오픈소스 사이트에 흩어진 CSS·Tailwind UI 컴포넌트를 수집하고, 코드와 메타데이터를 기반으로 검색할 수 있도록 구성한 플랫폼입니다. 사용자는 자연어와 카테고리 조건으로 원하는 컴포넌트를 탐색하고, 별도의 실행 환경을 만들지 않아도 브라우저에서 렌더링 결과와 원본 코드를 함께 확인할 수 있습니다.

| 항목 | 내용 |
| --- | --- |
| 기간 | 2025.10 ~ 2025.11 / 2인 팀 프로젝트 |
| 역할 | 크롤링·정제 파이프라인, 임베딩 저장 구조, FastAPI 백엔드 설계, React 검색·미리보기 UI 구현 |
| 기술 | Python, Selenium, FastAPI, PostgreSQL, pgvector, SentenceTransformers, React, Vite, Tailwind CSS |
| 구현 | UI 코드 수집·정제, 증분 임베딩 생성, 키워드·벡터 하이브리드 검색, iframe 미리보기, 카테고리별 렌더링 품질 필터 |
| 링크 | GitHub: https://github.com/suhwan855/csslibrarymaster |

---

## 서비스 흐름

**[검색·필터 화면] → [컴포넌트 미리보기 화면] → [코드 확인·활용 화면]**

**검색·필터 화면**

사용자가 자연어 검색어와 카테고리를 입력하면 관련 UI 컴포넌트 목록을 제공합니다. 컴포넌트 이름·설명의 키워드 유사도와 코드·메타데이터 임베딩의 벡터 유사도를 결합해 결과를 정렬하며, DB가 없는 환경에서도 주요 흐름을 확인할 수 있도록 동일한 API 응답 구조의 데모 모드를 제공합니다.

**컴포넌트 미리보기 화면**

검색 결과의 HTML·CSS 코드를 iframe 내부에서 렌더링합니다. 전체 HTML, 부분 HTML, CSS 중심 코드와 Tailwind 스니펫을 구분하고, 외부 폰트·아이콘 의존성을 감지해 필요한 리소스를 미리보기 문서에 주입합니다.

**코드 확인·활용 화면**

미리보기와 원본 코드를 전환하고 전체 화면으로 확인할 수 있습니다. 구문 강조와 줄 번호를 적용한 코드 뷰어 및 클립보드 복사 기능을 제공하며, 별도의 AI 생성 화면에서는 검색된 컴포넌트를 참고 문맥으로 활용해 HTML·CSS 결과를 스트리밍합니다.

---

## 1. 개발 배경

CSS와 Tailwind 기반 UI 스니펫은 GitHub나 컴포넌트 공유 사이트에 분산되어 있어 원하는 디자인을 비교하는 데 시간이 필요했습니다. 출처마다 코드 형식과 외부 의존성이 달랐고, 비슷한 컴포넌트가 반복적으로 노출되어 실제 프로젝트에 적용하기 전 별도의 검토와 수정이 필요했습니다.

이를 해결하기 위해 스니펫을 자동으로 수집·정제하고, 이름·카테고리·라이브러리·설명·코드를 검색 문맥으로 구성해 임베딩을 저장했습니다. 키워드 검색과 의미 기반 벡터 검색을 결합하고, 검색 결과는 격리된 iframe에서 직접 실행해 코드를 선택하기 전에 렌더링 상태를 확인할 수 있도록 구성했습니다.

---

## 2. 핵심 기능

### 오픈소스 UI 스니펫 수집

Selenium을 이용해 목록 페이지의 지연 로딩 콘텐츠를 스크롤로 불러오고, 정규식에 맞는 컴포넌트 상세 URL을 수집했습니다. 상세 페이지에서는 HTML과 CSS 탭을 각각 활성화한 뒤 에디터의 `textarea.value`와 일반 `pre code` 요소를 순서대로 검사해 코드를 추출했습니다.

### 코드 정제와 수집 실패 격리

수집한 코드에 포함된 HTML 엔티티와 불필요한 이스케이프 문자를 복원하고, 코드가 없는 항목은 저장 대상에서 제외했습니다. 상세 페이지 처리를 병렬화하되 DB 저장은 메인 흐름에서 관리하고, 개별 항목에서 오류가 발생해도 로그를 남긴 뒤 다음 항목을 계속 처리하도록 실패 범위를 분리했습니다.

### 임베딩 생성과 벡터 저장

컴포넌트 이름, 카테고리, 라이브러리, 설명, 코드를 하나의 검색 문맥으로 구성한 뒤 SentenceTransformers 모델로 임베딩을 생성했습니다. `embedding IS NULL`인 데이터만 처리해 작업 재실행 시 완료된 항목을 반복 계산하지 않고 새 항목과 실패 이후의 남은 항목부터 이어서 처리할 수 있도록 했습니다.

### 키워드·벡터 하이브리드 검색

FastAPI의 `/components/search` 엔드포인트에서 이름·설명에 대한 PostgreSQL `pg_trgm` 유사도와 pgvector 코사인 유사도를 계산합니다. 벡터 점수 65%, 키워드 점수 35%를 결합해 최종 순위를 정하고 카테고리와 라이브러리 조건을 함께 적용합니다. DB 연결 정보가 없으면 내장 샘플을 검색하는 데모 모드로 전환해 프론트엔드와 API의 실행 흐름을 유지합니다.

### 격리된 실시간 미리보기

수집된 코드를 React의 `iframe srcDoc`로 렌더링해 컴포넌트 스타일이 서비스의 부모 DOM에 영향을 주지 않도록 분리했습니다. 전체 HTML 문서, 부분 HTML, CSS 중심 코드, Tailwind 스니펫을 구분해 각각에 맞는 미리보기 문서를 생성합니다.

### 카테고리별 렌더링 품질 필터

버튼에만 고정된 검사가 아니라 Buttons, Inputs, Cards, Badges, Alerts 카테고리별 후보 DOM 선택자를 적용했습니다. iframe 내부 요소의 최소 크기와 `box-shadow`, `background-image`, `transform`, `filter`, 테두리, 애니메이션 등의 스타일 신호를 검사하고 기준을 만족하지 못한 결과는 목록에서 제외합니다.

---

## 3. 문제 해결 경험

### 출처마다 다른 코드 형식을 하나의 미리보기 환경으로 통합

**문제**

수집 대상마다 코드 제공 형식과 실행 조건이 달랐습니다. 일부는 완성된 HTML 문서였고, 일부는 CSS만 있거나 Tailwind 클래스와 외부 폰트·아이콘을 전제로 작성되어 그대로 렌더링하면 스타일이 깨지거나 부모 페이지에 CSS가 영향을 주는 문제가 있었습니다.

**해결**

렌더링 환경을 React `iframe srcDoc`로 통일해 각 컴포넌트를 독립된 문서에서 실행했습니다. 입력 코드를 전체 HTML, 부분 HTML, CSS 중심 코드로 구분하고, Tailwind 클래스가 감지된 경우에만 Tailwind CDN과 호환용 CSS를 추가했습니다. CSS만 존재해 실제 DOM이 없는 경우에는 CSS 패턴을 분석해 버튼과 컨테이너 요소를 합성했으며, 외부 폰트와 아이콘 의존성도 코드에서 감지해 iframe의 `<head>`에 주입했습니다. 렌더링 후에는 카테고리별 후보 요소와 계산된 스타일을 검사해 표시 여부를 결정했습니다.

**결과**

출처와 코드 형태가 달라도 동일한 React 컴포넌트에서 미리보기를 생성할 수 있게 됐습니다. 스니펫 스타일이 서비스 화면으로 누수되는 문제를 분리했고, DOM이 누락된 CSS 중심 코드도 실제 UI 형태로 확인할 수 있는 범위를 넓혔습니다. 또한 버튼 중심이던 품질 검사를 입력창·카드·배지·알림까지 확장했습니다.

### 동적 페이지와 코드 에디터 구조에 대응하는 크롤러 구현

**문제**

수집 대상 사이트는 목록을 스크롤에 따라 지연 로딩했고, 상세 페이지의 HTML과 CSS가 탭 내부 코드 에디터에 숨겨져 있었습니다. DOM 로딩 시점이나 에디터 구현 방식에 따라 단순 HTML 요청이나 단일 CSS 선택자만으로는 코드를 안정적으로 읽기 어려웠습니다.

**해결**

목록 페이지에서는 명시적 대기 후 일정 횟수만큼 스크롤해 지연 로딩된 링크를 확보하고, URL 패턴을 정규식으로 제한해 상세 컴포넌트 링크만 수집했습니다. 상세 페이지에서는 `aria-controls`로 탭과 코드 패널을 연결한 뒤 HTML·CSS 탭을 각각 활성화했습니다. 코드 추출은 React 기반 에디터의 `textarea.value`를 우선 확인하고, 값이 없을 때 일반적인 `pre code` 구조를 다시 검사하도록 폴백을 구성했습니다. 개별 URL에서 예외가 발생하면 로그를 남기고 다음 컴포넌트 처리를 계속하도록 실행 단위를 분리했습니다.

**결과**

지연 로딩과 탭 전환이 필요한 페이지에서도 HTML, CSS, 작성자와 출처 정보를 같은 흐름으로 수집할 수 있게 됐습니다. 한 상세 페이지의 DOM 변경이나 코드 누락이 전체 크롤링 작업을 중단시키지 않고 해당 항목만 건너뛰도록 동작합니다.

### 키워드와 의미 검색을 결합한 검색 흐름 구성

**문제**

이름이 정확히 일치하는 검색만으로는 “네온 효과가 있는 둥근 CTA 버튼”처럼 디자인 의도가 포함된 질의를 찾기 어려웠습니다. 반대로 벡터 유사도만 사용하면 사용자가 입력한 구체적인 컴포넌트 명칭이나 카테고리가 결과 순위에 충분히 반영되지 않을 수 있었습니다.

**해결**

PostgreSQL `pg_trgm`으로 이름·설명의 키워드 유사도를 계산하고, SentenceTransformers로 변환한 질의 벡터와 저장된 embedding의 코사인 유사도를 함께 계산했습니다. 두 점수를 0.35와 0.65의 가중치로 결합하고, 카테고리와 라이브러리 필터를 SQL 단계에서 적용했습니다. 프론트에서는 검색어와 카테고리를 URL 쿼리로 유지하고 동일한 응답 모델로 결과와 랭킹 방식을 표시했습니다.

**결과**

명확한 키워드 일치와 자연어 의미 유사도를 하나의 검색 결과로 제공할 수 있게 됐습니다. 또한 DB가 없는 로컬 환경에서는 데모 검색으로 자동 전환하도록 구성해 환경 설정 전에도 검색 UI, API 요청, iframe 미리보기까지 전체 흐름을 확인할 수 있습니다.

---

## 4. 아키텍처

```
오픈소스 UI 사이트
  ↓
Selenium 크롤러
  ↓
코드·메타데이터 파싱 및 정제
  ↓
PostgreSQL + pgvector + pg_trgm
  ↓
FastAPI 카테고리 조회·하이브리드 검색 API
  ↓
React + Vite 검색·필터 UI
  ↓
iframe srcDoc 미리보기 및 코드 표시
```

운영 DB가 연결된 환경에서는 PostgreSQL의 키워드·벡터 점수를 결합하고, DB가 없는 환경에서는 FastAPI가 내장 샘플을 동일한 응답 모델로 제공합니다. 임베딩 모델은 검색이 처음 요청될 때 지연 로딩해 일반 목록 API의 시작 비용을 줄였습니다. AI 생성 기능은 OpenAI API 키와 pgvector 검색용 DSN이 설정된 경우에만 활성화됩니다.

| 주요 API | 역할 | 구현 상태 |
| --- | --- | --- |
| 하이브리드 검색 API | 키워드·벡터 점수 결합, 카테고리·라이브러리 필터, 데모 fallback | `GET /components/search` 구현 |
| 카테고리 조회 API | 카테고리별 전체 수와 페이지 단위 컴포넌트 조회 | `GET /components` 구현 |
| 상태 확인 API | 백엔드 기동 여부 확인 | `GET /health` 구현 |
| AI HTML·CSS 생성 API | 유사 컴포넌트를 검색 문맥으로 사용해 결과를 스트리밍 | `POST /chat/stream` 구현 |

---

## 5. 테스트 및 결과

프론트엔드는 ESLint와 Vite 프로덕션 빌드로 정적 검증했습니다. `npm run lint`와 `npm run build`가 모두 통과했으며, 893개 모듈이 변환되어 배포용 번들이 생성되는 것을 확인했습니다.

백엔드는 FastAPI TestClient를 이용해 `/health`, `/components`, `/components/search`를 호출했습니다. DB 환경변수가 없는 상태에서 데모 모드가 활성화되고, 카테고리별 목록과 `네온` 검색 결과가 정상적인 JSON 응답으로 반환되는 것을 확인했습니다. Python 구문 컴파일과 `git diff --check`도 통과했습니다.

현재 검증 범위에는 프론트엔드 린트·빌드, 백엔드 핵심 API 응답, iframe 렌더링 분기와 카테고리별 스타일 품질 검사 로직이 포함됩니다. 실제 운영 데이터 기준의 검색 정확도, 크롤링 성공률과 응답 시간은 아직 별도의 정량 지표로 측정하지 않았습니다.

---

## 6. 한계 및 개선 방향

- 하이브리드 검색 가중치는 현재 벡터 0.65, 키워드 0.35로 설정했으며, 대표 질의와 정답 스니펫으로 평가 데이터셋을 구성해 Precision@K 또는 nDCG를 측정할 필요가 있습니다.
- 크롤러가 특정 DOM 선택자와 URL 형식에 의존하므로, 출처별 파서를 분리하고 선택자 변경을 감지하는 테스트를 추가해야 합니다.
- 임베딩을 행마다 커밋하는 현재 구조는 데이터가 많아질수록 DB 작업이 증가하므로, 배치 단위 인코딩과 일괄 업데이트로 개선할 필요가 있습니다.
- 외부 코드를 iframe에서 실행하므로, 필요한 기능을 유지하는 범위에서 sandbox 권한을 줄이고 CSP와 외부 리소스 허용 목록을 적용해야 합니다.
- 프론트엔드 프로덕션 번들이 약 1.8MB이므로 검색, 코드 하이라이터, AI 생성 화면을 동적 import로 분리해 초기 로딩 크기를 줄일 필요가 있습니다.

---
